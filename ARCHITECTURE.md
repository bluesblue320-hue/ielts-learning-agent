# Architecture document moved

The canonical architecture document is now [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md).

This root-level file is retained as a compatibility pointer so existing links do not silently break.
